<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data>

        <!-- Stock Location list View -->
        <record id="view_indent_stock_location_tree2" model="ir.ui.view">
            <field name="name">Indent Stock Location List</field>
            <field name="model">stock.location</field>
            <field name="inherit_id" ref="stock.view_location_tree2"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='usage']" position="after">
                    <field name="can_request" optional="hide"/>
                </xpath>
            </field>
        </record>

        <!-- Stock Location Form View -->
        <record id="view_indent_stock_location_form" model="ir.ui.view">
            <field name="name">Indent Stock Location Form</field>
            <field name="model">stock.location</field>
            <field name="inherit_id" ref="stock.view_location_form"/>
            <field name="arch" type="xml">
                <xpath expr="//group[1]" position="after">
                    <group string="Indent">
                        <field name="can_request"/>
                    </group>
                </xpath>
            </field>
        </record>

    </data>
</odoo>